server:
  port: 8001
management:
  endpoints:
    web:
      exposure:
        include: refresh,health
spring:
  application:
    name: easyblog
  cloud:
    nacos:
      config:
        server-addr: 117.50.189.250:8090
        file-extension: yml
        group: DEFAULT_GROUP